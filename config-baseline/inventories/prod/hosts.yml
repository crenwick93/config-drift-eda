---
all:
  children:
    managed_nodes:
      hosts:
        # Populate with Terraform outputs or static IPs.
        # AAP uses its own inventory; this file is for standalone testing.
        # rhel-managed-1:
        #   ansible_host: 10.0.1.10
        # rhel-managed-2:
        #   ansible_host: 10.0.1.11
      vars:
        ansible_user: ec2-user
        ansible_ssh_private_key_file: ~/.ssh/config-drift-key.pem

    webservers:
      hosts: {}
